'use client'

import { useEffect } from 'react';

export default function CreatorGalleryPageScripts() {
  useEffect(() => {
    // This page has minimal scripts - just the year update which is 
    // handled globally in the footer component
  }, []);

  return null; // This component doesn't render anything itself
}

